# SDD ledger — plan: docs/superpowers/plans/2026-09-23-window-sharing.md

Base: 2a7a0476829f3847c1376fb693b035b2c75bc171
Worktree: H:\Script\SonicRelay\dotnet_FrameRelay\.worktrees\issue-16-window-audio

Pre-flight interface scan:
- Task 1 produces typed CaptureTarget, WindowInfo, IWindowEnumerator, capture target-closed event, and typed host/pipeline startup; Task 2 consumes CaptureTarget/WindowInfo, Task 3 consumes CaptureTarget/WindowInfo and capture contracts, Task 4 consumes WindowInfo, Task 5 consumes targets/closure/source contracts, Task 6 consumes enumeration/selection contracts. Signatures agree after task-local file updates.
- Task 2 produces the Win32 window item creation path; Task 3 consumes an injectable GraphicsCaptureItem factory for monitor/window item creation. The plan originally said “both item factories” without defining them. Ruling: add IGraphicsCaptureItemFactory with CreateForMonitor(MonitorInfo) and CreateForWindow(WindowInfo) as Task 2 output — why: adapters need a seam for no-desktop lifecycle tests — cost if wrong: one small interface may be unnecessary.
- Task 3 produces GraphicsCaptureWindowSource and common WGC lifecycle; Task 5 consumes both as publisher implementations. Signatures align through IScreenCaptureSource after Task 1.
- Task 4 produces ProcessLoopbackAudioSource and its injected client factory; Task 5 consumes the audio source based on CaptureTarget.Window. Signatures align on WindowInfo.ProcessId and process-tree mode.
- Task 5 produces host target closure/audio diagnostics; Task 6 consumes host diagnostics and Task 7 verifies their fields. No remaining interface mismatch found.

Tool adaptation: this skill package has sdd-workspace/task-brief/review-package but no task-start/task-done scripts. Use task-brief for each task and manually record command/output and task completion in this ledger.
- Task 1: Ruling: add ScreenPublishPipeline.cs and RtcVideoPublishHost.cs to the task file list — generic capture start and the changed IVideoPublishHost must compile through the media pipeline and concrete app host in this task — cost if wrong: the task boundary carries two preparatory changes that could have been deferred.
Task 1: complete (commits 440ea12..HEAD, tests: dotnet test SonicDesktopRelay.Media.Tests → 78/78 pass; dotnet test SonicDesktopRelay.Presentation.Tests → 55/55 pass; dotnet build SonicDesktopRelay.App → 0 warnings, 0 errors). RED evidence: VideoContractsTests failed on missing CaptureTarget/WindowInfo; runtime tests failed on missing typed StartSharingAsync; pipeline window test failed on MonitorInfo-only pipeline API. Ruling: the plan omitted ScreenPublishPipeline and RtcVideoPublishHost from Task 1 files although the task changes those APIs; added them so the task commit remains buildable.
Task 2: complete (commits c2ff3ad..HEAD, tests: dotnet test SonicDesktopRelay.Media.Windows.Tests --filter WindowEnumeratorTests → 5/5 pass; full dotnet test SonicDesktopRelay.Media.Windows.Tests → 77/77 pass). RED evidence: first enumerator test build failed because IWindowApi was absent; item-factory routing test failed because GraphicsCaptureItemFactory was absent; same-PID reuse test failed before final process-start identity recheck. Implemented Win32 enumeration, test seams, HWND item creation at interop slot 3 and monitor/window item factory routing.
Task 3: complete (tests: `dotnet test tests/SonicDesktopRelay.Media.Windows.Tests/SonicDesktopRelay.Media.Windows.Tests.csproj` → 79/79 pass; `dotnet build src/SonicDesktopRelay.Media.Windows/SonicDesktopRelay.Media.Windows.csproj` → 0 warnings/errors). Extracted `GraphicsCaptureItemSource` as the shared D3D/WGC lifecycle; monitor and new window adapters forward frames, dimensions, rate changes, diagnostics and closure; window adapter validates HWND/PID/process start identity and monitors owner/window exit. Added no-desktop adapter tests for stale-window rejection before startup and typed start/stop forwarding. Ruling: the projected WinRT capture item/frame-pool/session APIs are concrete runtime classes and cannot be faked through ordinary interfaces; keep resource disposal/frame-pool resize/FPS integration behavior exercised by the existing real WGC source tests, while adding adapter-level injected contracts. This leaves native resource cleanup and mid-session resize dependent on Windows capture integration coverage.
Task 4: complete (tests: focused process/system loopback filter → 8/8 pass; `dotnet build src/SonicDesktopRelay.App/SonicDesktopRelay.App.csproj` → 0 warnings/errors). Added the process-tree source, normalized packet adapter, build 20348 guard, explicit degraded behavior with no global-loopback fallback, async process-loopback activation and WASAPI capture client. Injected-client tests cover tree/PID/format parameters, arbitrary chunk framing, silence-capable packets, stop detachment, process exit and activation failure. Native activation/device capture was compile-verified but not exercised against a real render stream in this environment.
